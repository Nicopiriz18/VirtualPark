services:
  sqlserver:
    image: mcr.microsoft.com/azure-sql-edge
    container_name: azure-sql-edge2
    ports:
      - "1433:1433"
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=Passw1rd
    volumes:
      - sqlvolume:/var/opt/mssql

volumes:
  sqlvolume:
